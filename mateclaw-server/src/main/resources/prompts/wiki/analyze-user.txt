## Título del documento

{raw_title}

## Índice de páginas Wiki existentes (para elegir la lista blanca de related_pages; cada línea con el formato `[[slug]] — Título — Resumen`)

{existing_pages}

## Contenido del documento (extracto, para el análisis de estructura global)

{text_sample}

---

Analiza el documento anterior y emite el mapa de conceptos en JSON. Solo emite JSON, sin bloques de código markdown.
- Cada slug de `related_pages` debe venir 100% del «Índice de páginas Wiki existentes» de arriba
- Si no hay páginas existentes relacionadas, `related_pages` devuelve `[]`
