## Reglas de procesamiento de la base de conocimiento

{config}

{document_map_section}

## Índice de páginas Wiki existentes (garantía fuerte, puedes enlazar directamente; cada línea con el formato `[[slug]] — Título — Resumen`)

{existing_pages}

## Páginas que se crearán en este mismo lote (en planificación, pueden ser enlazables; el slug está en pages_to_create abajo)

Enlazar a este tipo de páginas **no tiene garantía de éxito**: las páginas de este lote pueden no llegar a persistirse por deduplicación / fusión / fallo, y el `[[slug]]` correspondiente se marcará como enlace muerto en el lint, que un humano corregirá después. Cuando necesites referencias cruzadas, **prioriza** enlazar a las páginas del «Índice de páginas Wiki existentes» de arriba.

```json
{pages_to_create}
```

## Material crudo

Título: {raw_title}

{raw_content}

---

Genera el contenido markdown completo para **cada página** del arreglo `pages_to_create` de arriba.
- Emite según el formato de bloques FILE definido en el mensaje de sistema, un bloque FILE por página
- El contenido de cada página debe basarse en la información del material crudo relacionada con ese tema
- Usa apropiadamente [[slug]] para enlazar a páginas relacionadas (el slug debe venir del «Índice de páginas Wiki existentes» de arriba o de los slugs de este lote `pages_to_create`)
- No omitas ninguna página
