# AuraClaw — Customization Registry & Sync Procedure

> Documento de control para el fork comercial **AuraClaw** (Auracore SAS) sobre **MateClaw** (Apache-2.0, https://github.com/mateaix/mateclaw).

## 1. Estrategia de ramas y remotes

```
origin   → git@github.com-auracore-sas:auracore-sas/auraclaw.git   (fork privado — NUESTRO repo)
upstream → https://github.com/mateaix/mateclaw.git                 (repo oficial, solo lectura)

main     → línea comercial estable (base: tag v2.1.0 del upstream)
dev      → referencia local de la rama de desarrollo del upstream (no trabajamos aquí)
```

**Reglas:**
- ⛔ NUNCA usar el botón **"Sync fork"** de GitHub en `main` (el `main` del upstream es una línea
  curada/aplanada que no contiene los tags; mezclarlo rompería nuestra historia).
- ⛔ Nunca hacer `rebase` de `main` — solo `merge` (historia compartida con deploys/tags).
- ✅ La sincronización SIEMPRE es por CLI con tags estables (ver sección 3).
- ✅ `git rerere` está habilitado (`git config rerere.enabled true`): memoriza cómo resolvimos
  conflictos de branding y los re-aplica automáticamente en merges futuros.

## 2. Registro de personalizaciones (archivos tocados)

| Archivo | Cambio | Manejo de conflicto en merge |
|---|---|---|
| `README.md` | Marca renombrada: `MateClaw` → `AuraClaw` (solo prosa); aviso de fork agregado arriba | Tomar nuestra versión y re-aplicar: `sed -i 's/MateClaw/AuraClaw/g' README.md` |
| Logo / branding visual | Binarios reemplazados **en su lugar** con nombres originales (`mateclaw_logo.png`, `mateclaw_logo_s.png`, `favicon.ico`, `build/icon.*`) — contenido AuraClaw; imagen maestra en `assets/branding/auraclaw.png`; **cero archivos fuente modificados** por el logo | Si upstream cambia el binario del logo: conflicto binario → resolver manualmente: `git checkout --ours <ruta>` + `git add` (rerere NO aplica a binarios) |
| `mateclaw-ui/index.html`, i18n `en-US.ts`/`zh-CN.ts`, vistas (Login, About, MainLayout, chat), `types/index.ts`, `WorkflowJsonEditor.vue` + **logos de dos tonos (2026-08-21)** — `Login.vue` (logo-title), `MainLayout.vue` (logo-name, mobile-topbar-title), `MessageList.vue` (welcome-title), `About/index.vue` (hero-title), `mateclaw-webchat/src/index.ts` (DEFAULT_CONFIG.title), `mateclaw-desktop/src/App.vue` (alt del logo + `AuraClaw Desktop v{{appVersion}}`) | Textos visibles: `MateClaw` → `AuraClaw` | Re-aplicar `sed 's/MateClaw/AuraClaw/g'` sobre estos archivos si upstream los toca. ⚠️ El `sed` NO cubre el patrón de dos tonos `Mate<span …>Claw</span>` (quedó como `Aura<span …>Claw</span>`) — re-aplicar manualmente en los 5 archivos listados |
| **i18n español** — `es-ES.ts` (nuevo, 3,825 strings) + `i18n/index.ts` (default `es-ES`) + `App.vue` (element-plus es) + `MainLayout`/`System` (opción ES) + `SystemSettingService.java` (default servidor `es-ES`) | Español general (latinoamericano neutro, tú informal) | `es-ES.ts` es aditivo (sin conflicto). `index.ts`/`App.vue`/`MainLayout`/`System` tienen ~3 líneas nuestras: re-aplicar si upstream los toca. `SystemSettingService.java` línea del default: conservar nuestra versión |
| **Archivos de memoria de agentes en español** — `SOUL.md`/`PROFILE.md`/`MEMORY.md` de los 3 agentes semilla (traducidos en `data-*-es.sql` y aplicados a la BD viva, ids 1000200002-24). Los `AGENTS.md` (guías de comportamiento, ~25KB) siguen en inglés/chino: pendiente de traducir | Archivos dentro de los seeds es (sin conflicto). BD viva: ya actualizada |
| **Seeds de BD en español** — `data-es.sql`, `data-kingbase-es.sql`, `data-mysql-es.sql` (nuevos, traducidos de los `-en`) + `DatabaseBootstrapRunner.java` (rama `es-ES` → `data-*-es.sql`) | Instalaciones nuevas con locale es-ES reciben agentes, tools, skills, cron y settings en español (los prompts de agentes indican responder en español) | Archivos nuevos = aditivos, sin conflicto. `DatabaseBootstrapRunner.java` tiene ~4 líneas nuestras: re-aplicar si upstream lo toca. BD existentes: traducción aplicada manualmente vía UPDATEs (ver registro de la sesión) |
| `mateclaw-desktop/branding.config.json` | Nombre/appId/equipo/copyright → AuraClaw (appId: `com.auracore.auraclaw`) | Tomar nuestra versión |
| `AGENTS.md` | Contexto para agentes de codificación (nuestro, no existe en upstream) | Sin conflicto (archivo nuevo). Si upstream crea el suyo: conservar NUESTRO |
| `docs/CUSTOMIZATIONS.md` | Este archivo (nuestro, no existe en upstream) | Sin conflicto (archivo nuevo) |
| **Documentación en español (COMPLETA)** — `mateclaw-server/src/main/resources/docs/es/*.md` (40 archivos, ~860KB): mismo slug que `en/`, traducción completa de los 34 documentos pendientes + los 6 de la fase inicial. Enlaces `./slug` con anclas solo cuando el header queda en inglés. Términos técnicos sin traducir (ReAct, Tool Guard, workspace, sidecar…); marcadores de protocolo en chino preservados. **Fallback es→en (P3, 2026-08-21):** `MateClawDocService.read()` sirve la versión `en/` si el slug no existe en `es/`; `list()` marca esos docs con `fallback=true` y la UI muestra un badge EN (`Docs/index.vue` + `DocMeta.fallback`) | Traducción neutra (latinoamérica, tú informal) | Archivos nuevos = aditivos, sin conflicto. Si upstream agrega/renombra slugs en `en/`, replicar en `es/`. `MateClawDocService.VALID_LANG` ya incluye `es` (líneas nuestras, re-aplicar si upstream lo toca); el fallback es→en y el campo `fallback` del `DocMeta` record también son nuestros (re-aplicar en merge) |
| **Marcadores de protocolo en español (P4/P5, cambio COORDINADO)** — los marcadores visibles/parseables se emiten ahora en español: `[Error]` (antes `[错误]`), `Fuentes:` (antes `来源：`), `[Pendiente de aprobación]` (antes `[等待审批]`), `[Instrucción de tarea]` (antes `[任务指令]`), `[Resumen de observaciones de herramientas]` (antes `[工具观察摘要]`). Todos los parsers (backend `ChannelErrorClassifier.hasErrorPrefix`, `ApprovalPlaceholderUtil`, `PlanGenerationNode`, `SourceEvidenceLedger`; frontend `chatError.ts`, `useMarkdownRenderer.ts`) aceptan **ambos idiomas** (chino legacy + español) para no romper BD histórica ni streams en vuelo. Templates de error LLM y los 60 prompts `.txt` de `prompts/` traducidos al español (contratos JSON/`[[slug]]`/FILE-block/placeholders preservados). Mensajes `[已中断]`/`[已停止生成]` se muestran vía status+i18n (no se cambiaron) | Bilingüe-tolerante. Re-aplicar si upstream toca: las constantes en `ChannelErrorClassifier`, `ApprovalPlaceholderUtil`, `PlanGenerationNode`, `CronJobRunner`, `SourceEvidenceLedger`, `WikiContextService`, `NodeStreamingChatHelper`, `SummarizingNode`, `DelegateAgentTool`, adaptadores Feishu/DingTalk, y los parsers del frontend (`chatError.ts`, `useMarkdownRenderer.ts`, `MessageBubble.vue`, `PlanDetailPanel.vue`, `PlanBoard.vue`). Nunca eliminar la variante legacy del parsing |
| **Verificación visual P4/P5 (completada 2026-08-21) + fixes de renderizado bilingüe** — verificación en browser real (Playwright) de los 5 marcadores con datos sintéticos en BD de dev. Se encontraron y corrigieron 3 gaps del frontend que rompían la tolerancia bilingüe: (1) `MessageBubble.vue` solo ocultaba el texto crudo `[错误]` (no `[Error]`) cuando hay tarjeta de error → texto duplicado en errores en español; (2) `isApprovalPlaceholder` no incluía `[Pendiente de aprobación]` ni el stub legacy `[本次没有输出]` → texto crudo visible en historial; (3) `cleanGoal` de `PlanDetailPanel.vue`/`PlanBoard.vue` solo limpiaba `[任务指令]`, no `[Instrucción de tarea]` → prefijo visible en títulos de planes. Verificado: tarjetas de error (ES+legacy), stub de aprobación oculto, `Fuentes:`/`来源：` y títulos de planes renderizan correctamente. Tests UI: 306 pass / 8 fail preexistentes (sin regresiones) | Bilingüe-tolerante; mismo manejo que la fila anterior |
| **Plantilla PROFILE.md del usuario (corrección de ambigüedad)** — los `PROFILE.md` de los agentes semilla tenían una sección `## Identidad` con `Nombre/Rol/Estilo` sin aclarar que es del usuario; combinado con el `ABOUT_YOU_BLOCK` del system prompt, el agente se escribía a sí mismo (`AuraClaw Assistant`) en lugar de capturar los datos del usuario. Corregido: la sección se fusionó en `## Perfil del Usuario` con una nota `> Este archivo describe al USUARIO...`. Aplicado a seeds `data-*-es.sql` (3 agentes) y a la BD viva (5 filas via UPDATE, 2026-08-21). Los seeds `en/zh` mantienen la plantilla ambigua (pendiente opcional). El agente ahora usa `remember_structured` (type=user) para guardar datos del usuario | Baja superficie. Re-aplicar en seeds `en/zh` por coherencia si se quiere. BD viva: solo UPDATEs, no recargar seeds |
| **V900 · Uso de modelos por propósito (`usage_scope`)** — feature de aislación de modelos: un modelo `chat` puede dedicarse a trabajos internos (p. ej. digestión Wiki) y quedar **excluido del chat normal** (default, failover, pin, pickers), mientras sigue siendo seleccionable por id en el wiki. Implementado: migración `V900__model_usage_scope.sql` (3 dialectos, columna `usage_scope` JSON array), `ModelConfigService.isChatUsable()` + filtros en `listEnabledModels`/`getDefaultModel`/`resolveModel`/`getDefaultModelByProvider`/`getPrimaryChatModelByProvider`/`findEnabledModel` (pin) + rechazo en `setDefaultModel` (×2 overloads); `AgentGraphBuilder` (failover `pickFallbackModel`/`findFirstAvailableChatModel`); `ConversationController.setModel` rechaza el pin de conversación a un modelo dedicado (400 con mensaje claro); DTO `ModelInfoDTO` expone `usageScope`+`chatEligible`; endpoint `PUT /models/{providerId}/models/usage-scope`; UI en `ManageModelsModal.vue` (badges + editor Chat/Wiki) + `ModelSelector.vue` (filtra `chatEligible === false` del selector de chat) + i18n 3 idiomas. La wiki **no cambió**: usa `getModel(id)` (ignora enabled/usage) y su fallback interno `isUsableChatFallback` sigue aceptando modelos dedicados. Semántica: `usage_scope` NULL/vacío = legacy chat-usable; sin `"chat"` en el array = solo tareas internas; parseo fail-open (scope inválido → chat-usable). Tests: `ModelConfigServiceUsageScopeTest` (12) | **Superficie caliente moderada**: `ModelConfigService` + `AgentGraphBuilder` (failover) + `ConversationController` + `ModelSelector.vue`. Re-aplicar en merge si upstream toca esos métodos. Migración V900 = NUESTRA numeración, no renumerar. La BD dev (docker) migrará sola al arrancar (flyway) |
| **Módulo Enterprise deshabilitado** — `mateclaw-ui/src/router/index.ts` + `mateclaw-ui/src/views/layout/MainLayout.vue` (eliminada la entrada de menú y la ruta `/enterprise`) | El "Workbench de IA Empresarial" (`views/Enterprise/*.vue`) es una demo estática de frontend: datos mock hardcodeados, botones sin `@click`, **sin backend**. Se retira del menú lateral y del router para que quede inaccesible. Se dejan en disco sin uso los componentes `views/Enterprise/*.vue` y las strings i18n (`nav.enterprise`, bloque `enterprise.*` en los 3 locales) | Re-aplicar si upstream reintroduce/toca esas entradas: eliminar el `path: 'enterprise'` del router y el ítem `path: '/enterprise'` de `MainLayout.vue`. Si upstream borra/reescribe `views/Enterprise/`, aceptar su versión (no la usamos). ⚠️ NO confundir con el grupo de canales "enterprise" de `ChannelTypePicker.vue` (categoría IM empresarial, esa SÍ se mantiene) |
| **Fix GPT-5 en chat (`max_tokens` → `max_completion_tokens`, 2026-08-26)** — bug del código base: `ReasoningNode.buildChatOptions` enviaba `max_tokens` para todos los modelos OpenAI-compatibles, pero OpenAI rechaza `max_tokens` para la familia gpt-5* con 400 (`Unsupported parameter: 'max_tokens'... Use 'max_completion_tokens' instead`). El Wiki sí lo respetaba (vía `ModelFamily.suppressMaxTokens()`), el chat del agente no. Fix centralizado: método `OpenAiRequestRewriter.translateMaxTokensForGpt5()` (safety net que convierte `max_tokens` → `max_completion_tokens` cuando `request.model()` es `gpt-5*`) aplicado en los 2 puntos de salida de `OpenAiCompatibleChatModelBuilder` (`chatCompletionEntity` y `chatCompletionStream`). Test nuevo `TranslateMaxTokensForGpt5Test` (6 casos). Se arregló además un test roto preexistente (`ConversationControllerBatchDeleteTest`: constructor sin `ModelConfigService`) que bloqueaba `mvn test` | **Baja superficie**: `OpenAiRequestRewriter` (método nuevo aditivo) + 2 líneas en `OpenAiCompatibleChatModelBuilder` + 2 tests. Re-aplicar si upstream toca el rewriter o el builder. Verificado en vivo: chat con gpt-5-mini responde con citas `[n]` + `Fuentes:` sin 400 |
| **Citas de Wiki en el system prompt (2026-08-26)** — GPT-4o (vía OmniRoute u OpenAI directo) respondía correctamente pero omitía los marcadores `[1]`/tabla `Fuentes:` (aviso `[证据不足] missing wiki citation [n]`), mientras GPT-5.x citaba bien. Test A/B demostró que el modelo Y el router citan perfectamente cuando la instrucción está en el system prompt: el problema era que la instrucción solo vivía en las descripciones de las tools del Wiki (enterrada entre 115 tool schemas). Fix: bloque `CITATION_FORMAT_BLOCK` en `AgentGraphBuilder.buildEnhancedPrompt` (formato canónico: marcadores inline `[n]` + sección `Fuentes:` al final + "nunca citar fuentes no leídas") + hints reforzados en `WikiTool` (`CITATION_FORMAT_HINT` en `wiki_read_page`/`wiki_search_pages`/`wiki_semantic_search` + `citationHint` con ejemplo concreto en español) | **Baja superficie**: constante nueva en `AgentGraphBuilder` (1 línea de concatenación) + constantes/hints en `WikiTool`. Re-aplicar si upstream toca `buildEnhancedPrompt` o las descriptions de WikiTool. Verificado en vivo: gpt-4o vía omniroute responde con `[1]` + `Fuentes:` sin aviso de evidencia |
| **Safety net de citas de Wiki (RFC-052 follow-up, 2026-08-26)** — aun con el system prompt, gpt-4o es inconsistente: a veces emite `[1]`+tabla, a veces `[[Título]]` (wiki-link), a veces `[Título](slug)` (markdown link) o título a secas bajo `Fuentes:`. Como el `SourceEvidenceLedger` ya registra las páginas realmente leídas en la ronda, ahora `appendWikiSourceTable` completa la tabla canónica `[1] Título` desde el ledger cuando la respuesta menciona (en cualquier formato nativo) una página verificada, y el aviso `missing wiki citation [n]` solo se emite cuando NO hay mención verificable (caso real de alucinación). Detección: wiki-links `[[...]]` + markdown links `[...](...)` + líneas crudas bajo `Fuentes:`/`来源：`, con normalización case/accent-insensitive (slugs con guiones también matchean). Índices de la tabla anexada = índices del ledger (para que `wikiCitation(index)` y `matchesSourceLine` coincidan). Tests: 5 nuevos en `SourceEvidenceLedgerTest` (20 total) | **Superficie baja-media**: `SourceEvidenceLedger` (nuevo branch en `appendWikiSourceTable` + 3 helpers privados). Re-aplicar si upstream toca el ledger. Verificado en vivo: gpt-4o vía omniroute (consulta de ubicaciones) → respuesta completa + tabla canónica anexada + SIN aviso |
| **Canales individuales (V901, 2026-08-27)** — comportamiento upstream: las conversaciones de canales IM se crean con `username=system` y son visibles para TODOS los usuarios del workspace (`applyOwnerScope` incluye `system`). Ahora `mate_channel.owner_username` (NULL=compartido/legacy) captura al usuario autenticado que crea el canal; las conversaciones del canal se crean/conversan con ese owner **sticky** (`getOrCreateSharedConversation` 6-arg + bloque de corrección de owner respeta el owner), así solo el dueño las ve en su sidebar (`applyOwnerScope` ya filtra por username) — los admins globales siguen viendo todo (`isConversationOwner`). Backfill aplicado: canal Telegram 2092623667826462721 → `admin` (según audit CREATE) + conversación `telegram:...:1989192375` → `admin`. Verificado en vivo: admin ve la conversación, ebermeo no. Tests: `ConversationServiceChannelOwnerTest` (6 nuevos) + mocks de router actualizados a 6-arg (46 tests del área OK) | **Superficie baja**: `ChannelEntity` (+1 campo), `ChannelController` (create captura `Authentication`, update preserva owner), `ConversationService` (overload 6-arg, owner sticky), `ChannelMessageRouter` (2 call sites pasan owner), migraciones V901 (h2/kingbase/mysql). Re-aplicar si upstream toca esos archivos. Nota: canales creados ANTES de V901 quedan sin owner (compartidos) hasta que se recreen o se les asigne owner por SQL |
| **Voz entrante en Telegram (V902, 2026-08-27)** — el adapter de Telegram recibía las notas de voz (`voice` → `MessageContentPart.audio(fileId, "voice.ogg")`, inputMode="voice") pero **nunca las descargaba ni transcribía**; el LLM solo veía el placeholder y respondía "no hay transcripción disponible". Ahora: `TelegramChannelAdapter` descarga el audio (dos llamadas: `getFile` → `file_path` → `https://api.telegram.org/file/bot<token>/<path>`) y lo transcribe vía `SttService` (inyectado desde `ChannelManager`, que ya lo tenía para Feishu), inyectando el texto como parte de texto adicional + `textContent`. Best-effort: si STT falla/está deshabilitado queda el placeholder legacy `[语音]` y el mensaje sigue. inputMode sigue "voice" (respuesta corta/oral). STT usa los proveedores ya configurados en Ajustes (hoy: openai/whisper-1; el transporte es OpenAI-compatible → sirve para whisper.cpp local o cualquier STT compatible, apiKey opcional) | **Superficie baja**: `TelegramChannelAdapter` (constructor +1 param `SttService`, 2 métodos nuevos package-private, bloque de voz) + `ChannelManager` (1 línea) + tests (`TelegramVoiceTranscriptionTest` 6 casos; `SingleLeaderHookTest` 5 call sites con `null`). Re-aplicar si upstream toca el adapter/ChannelManager. Verificado en vivo: descarga + whisper-1 del voice note real → "Revisa por favor si este audio lo puedes analizar." |
| **Gráficas en Telegram (V902, 2026-08-27)** — dos problemas: (1) el agente pedía "gráfica" y respondía "se renderiza automáticamente arriba" sin generar nada (alucinación: los links a `/api/v1/files/generated/{id}` solo los muestra la web, Telegram no renderiza nada automático); (2) aun generando la imagen, Telegram recibía el link markdown a `127.0.0.1` (inalcanzable) porque el adapter no subía adjuntos nativos. Fix: (a) `CHART_DELIVERY_BLOCK` en `AgentGraphBuilder.buildEnhancedPrompt` (todos los agentes): si el usuario pide gráfica/visualización, GENERAR imagen (html_image_render) e incluir la URL — nunca afirmar que "se renderiza arriba"; (b) prompt del Asistente General en BD (guía de gráficas); (c) `TelegramChannelAdapter.renderAndSend` override: desenvuelve links markdown hacia URLs generadas (`[label](url)` → `label`), escanea con `GeneratedFileScrubber` (texto con marcador 📎 + bytes) y sube los bytes como `sendPhoto`/`sendDocument` nativo vía multipart (nuevo `sendTelegramMediaBytes`). `GeneratedFileScrubber` inyectado desde `ChannelManager` (ya existía para WeCom/Feishu/DingTalk — Telegram no lo usaba) | **Superficie media-baja**: `AgentGraphBuilder` (bloque constante + 1 línea concatenación), `TelegramChannelAdapter` (override renderAndSend + 2 helpers + constructor +1 param), `ChannelManager` (1 línea), tests (`unwrapGeneratedLinks` 3 casos). Re-aplicar si upstream toca esos archivos. Verificado en vivo: agente generó `pie_tipo_identificacion.png` con `render_html_image` + upload multipart real a Telegram (message_id 37). Web ya mostraba la imagen; Telegram ahora también |
| **Tablas markdown en Telegram (V902, 2026-08-27)** — Telegram no soporta tablas markdown (ni Markdown legacy ni MarkdownV2): se muestran desalineadas o como texto plano con pipes. Fix: nuevo `MarkdownTableFormatter` (utilidad en `channel/media`, reutilizable por otros canales IM) que detecta runs de tabla markdown (líneas `|` con línea separadora `|---|---|`) y las convierte en un bloque de código monospace (```code fence```, que Telegram sí renderiza en monospace) con columnas alineadas por ancho máximo, marcado inline de celdas limpiado (`**bold**`/`` `code` ``/`[link](url)`/`_itálica_` → texto), pipes escapados `\|` respetados, y separador visual header/cuerpo. Conectado en `TelegramChannelAdapter.sendMessage` + `sendThreadedText` (punto único de salida: cubre replies, proactivos y narraciones). Prosa con pipes sueltos sin separador NO se toca (detección conservadora). Tests: `MarkdownTableFormatterTest` 9 casos | **Superficie baja**: clase nueva `MarkdownTableFormatter` + 2 líneas en el adapter (sendMessage/sendThreadedText) + import. Re-aplicar si upstream toca el adapter. Verificado: salida real con la tabla de la conversación (columnas alineadas en fence). Otros canales sin soporte de tablas (qq/slack/discord/weixin) pueden reutilizar la misma utilidad |
| **Fix entrega de gráficas + tablas en celular (V902, 2026-08-27)** — bugs detectados en pruebas reales: (1) la gráfica no llegaba como foto: `renderAndSend` hacía `scrub(unwrapGeneratedLinks(content))` — el unwrap ELIMINABA la URL generada antes de que el scrubber pudiera extraer los bytes → attachments vacíos y el texto quedaba con `!nombre.png` (el `!` del link de imagen pegado al label). Fix: orden invertido — scrub primero (extrae bytes, reemplaza URL → `📎 archivo`), luego `cleanScrubbedLinks` limpia el markdown resultante (`[label](📎 archivo)` / `![label](📎 archivo)` → `label`; `[label](⚠️ aviso)` → `⚠️ aviso` legible). (2) tablas anchas ilegibles en pantalla pequeña: `MarkdownTableFormatter` ahora trunca celdas a 16 chars de ancho visual (code points, emojis/CJK cuentan doble) y si la tabla renderizada supera 34 chars la colapsa a viñetas `• etiqueta: valor · valor` (más legible en celular que monospace con scroll). (3) loop del agente al re-pedir la gráfica (intentaba localizar/reenviar el archivo, matplotlib/PIL/rutas): instrucción añadida a `CHART_DELIVERY_BLOCK` + prompt del Asistente General en BD — incluir la URL es suficiente, la plataforma la entrega como imagen nativa; nunca buscar/reenviar/regenerar el archivo | **Superficie baja**: `TelegramChannelAdapter` (orden del renderAndSend + `cleanScrubbedLinks` reemplaza a `unwrapGeneratedLinks`), `MarkdownTableFormatter` (truncado + bullets), `AgentGraphBuilder` (1 bullet en el bloque), prompt BD. Tests: `cleanScrubbedLinks` 4 casos, formatter +2 casos (bullets, truncado) — 24/24 OK. Verificado desplegado; pendiente confirmación del usuario con una gráfica nueva |
| **Panel por usuario + secciones admin-only (V902, 2026-08-27)** — Opción A acordada: el Panel (`/dashboard`) de un miembro normal mostraba el consolidado de TODOS los usuarios (conversaciones, mensajes, tokens, tool calls del workspace entero). Fix backend: `DashboardController.overview/trend` resuelven el usuario autenticado (`effectiveUsername`: admin global → null = consolidado; resto → su username) y `DashboardService` filtra conversaciones por `username` (conteo + resolución de IDs → mensajes/tokens también acotados). Fix admin-only: `cron-runs` y `cron-runs/{id}` pasan a `@RequireGlobalAdmin` (403 para miembros). Fix frontend (`Dashboard.vue`): `isAdminRole` (patrón localStorage del MainLayout) oculta la tarjeta de Modelos LLM y la tabla de Ejecuciones de cron para no-admins; el fetch de esos endpoints es condicional (un 403 habría tumbado el Promise.all). Nota: `Ajustes → Uso de Tokens` (`TokenUsageService`) sigue siendo global (sin filtro de usuario/workspace) — pendiente si se quiere acotar también | **Superficie baja**: `DashboardController` (helper + 2 endpoints + 2 anotaciones), `DashboardService` (overloads + filtro en queryStats), `Dashboard.vue` (v-if + onMounted condicional). Tests: `DashboardServiceUserScopedTest` 4 casos (filtro en conteo e IDs, null=workspace, username solo, cero sin conversaciones). Verificado en vivo con usuario temporal member: overview 0/0/0 vs admin consolidado, cron 403 vs 200 |
| **Token Usage por usuario (V902, 2026-08-27)** — `Ajustes → Uso de Tokens` (`/api/v1/token-usage`) era global (filtraba solo por fecha/modelo/proveedor, sin usuario ni workspace). Aplicado el mismo patrón del Panel: `TokenUsageController.effectiveUsername` (admin global → null; resto → su username) + `TokenUsageService.getSummary` 5-arg resuelve las conversaciones del usuario (MessageEntity no lleva username) y filtra mensajes por `IN conversationId`; sin conversaciones → resumen vacío. El overload legacy (4-arg) queda intacto para `OperationalDataExportService` (admin-only). Tests: `TokenUsageServiceUserScopedTest` 3 casos. Verificado en vivo con usuario temporal: 0/0/0 vs admin 105 msgs/10.5M tokens | **Superficie baja**: `TokenUsageController` (+helper +1 arg), `TokenUsageService` (+ConversationMapper +overload 5-arg +filtro). Re-aplicar si upstream toca estos archivos. Nota: el primer deploy del controller quedó llamando al overload legacy (bug de edición, la verificación en vivo lo cazó) — corregido y re-desplegado |
| **Fix loop MCP + schemas en caché (V902, 2026-09-01)** — dos bugs de integración MCP: (1) `ProgressAwareMcpToolCallback.serializeResult` descartaba `structuredContent` (solo concatenaba `content` → el LLM veía el resumen sin detalles y reintentaba la misma tool hasta 100 iteraciones — lentitud severa en el chat); ahora añade `[Detalle estructurado] <JSON>` con `structuredContent` cuando existe (dedupe semántico). (2) `McpServerService.serializeToolsCache` usaba hutool `JSONUtil.toJsonStr` que no serializa records de Java → `inputSchema` quedaba `{}` en el caché y el picker de la UI; ahora usa Jackson (`ObjectMapper` inyectado) tanto para el record como para la lista final | **Superficie baja**: `ProgressAwareMcpToolCallback` (+helper +dedupe semántico), `McpServerService` (+campo ObjectMapper, serializeToolsCache con Jackson). Re-aplicar si upstream toca estos archivos. Tests: +5 `ProgressAwareMcpToolCallbackTest`, +1 `McpServerServiceListToolsTest` (127/127 MCP OK). Relacionado: proxy local `powerfin_mcp_proxy.py` (systemd user `powerfin-mcp-proxy`) fuera del repo — bridge para servidores MCP stateless (GET 405 + negociación protocolVersion + `data`→`structuredContent`); ver `NEXT_SESSION.md` sesión 10ª-b |
| **Presupuesto de esquemas de tools en `docker-compose.yml` (2026-09-15)** — se commitean `MATECLAW_TOOL_SCHEMA_MAX_TOKENS` (default 40000) y `MATECLAW_CONTEXT_PREFIX_BUDGET_TOOL_SCHEMA_RATIO` (default 0.30) en el bloque `environment` de `mateclaw-server`, con comentario explicativo. Antes vivían solo en `.env` + `docker-compose.override.yml` (ambos gitignoreados) → un `docker compose up` limpio partía de los defaults de Spring (12000 / 0.25) y volvía a degradar `execute_sql` / `query_datasource` al catálogo de extensión. El ratio usa el **nombre canónico** (`mateclaw.context.prefix-budget.tool-schema-ratio` → `MATECLAW_CONTEXT_PREFIX_BUDGET_TOOL_SCHEMA_RATIO`); el alias antiguo `MATECLAW_TOOL_SCHEMA_RATIO` quedó retirado de `.env`. El override conserva solo los puertos de dev | **Superficie media**: es un archivo del upstream (`docker-compose.yml`, idéntico a `v2.1.0` antes de este cambio). En un merge con upstream, conservar NUESTRO bloque de las 2 vars (y el comentario) — es aditivo, el resto del archivo se puede tomar del upstream. Verificar con `docker compose config \| grep TOOL_SCHEMA` → 40000 / 0.30, también con `--env-file` vacío |
| **Tests alineados con branding/marcadores en español (2026-09-15)** — la suite completa del server (`4794` tests) tenía **12 fallos** en 5 clases, todos causados por nuestras personalizaciones (branding `AuraClaw` + marcadores/prompts en español), no por upstream: `AgentGraphBuilderIdentityBlockTest` (aseraba `MateClaw`), `ChatControllerPersistStatusTest` (`[等待审批]` → `[Pendiente de aprobación]`), `FeishuProcessStreamTest` (`startsWith("[错误]")`), `DelegateAgentToolDenyListTest` + `DelegateAgentToolTest` (8 asserts con `[错误]`/`上限`/`校验失败`/`最多`). Fix: las aserciones de **prefijo de error** ahora usan `ChannelErrorClassifier.hasErrorPrefix()` (contrato real, bilingüe, insensible al idioma) en vez de literales; las de contenido pasaron a los textos españoles emitidos. Además se cerró un gap de traducción en `DelegateAgentTool` (el mensaje de spawn-paused estaba en inglés en 2 de 3 sitios) → los tres ahora en español | **Superficie media**: 5 archivos de test del upstream + 3 líneas de `DelegateAgentTool`. En un merge con upstream, si upstream toca esos tests, conservar nuestras aserciones por `ChannelErrorClassifier` (los literales chinos/ingleses son exactamente lo que rompe). Re-aplicar tras cambiar cualquier marcador |
| **`.github/workflows/ci.yml` (2026-09-15)** — pipeline propio: el upstream NO trae workflows, aunque su `Dockerfile` asume uno (comentario: *"type errors are caught in CI, not in the image"*). Jobs: `server` (JDK 21 + `mvn -N install` + `plugin-api` + compile + suite completa, sube surefire-reports si falla), `ui` (pnpm 10 frozen + `vue-tsc --noEmit` + vitest + `vite build`) y `desktop` (solo `workflow_dispatch` con input `package_desktop`, macOS sin firma, **aún no validado**); **2026-09-21:** se añaden dos jobs manuales más, `desktop-linux` (ubuntu-latest + AppImage) y `desktop-windows` (windows-latest + NSIS), con sus propios inputs `package_desktop_*`, y el job `ui` gana un paso `pnpm run lint:precision` (el checker de Snowflake ya existe en el paquete, ver fila propia). Los dos jobs nuevos **siguen sin validar**: el primer `workflow_dispatch` con su flag es el que los valida. Dispara en push/PR a `main`, nightly y manual. Coste medido: suite completa ≈ 15 min en 8 cores | Archivo nuevo nuestro (`.github/workflows/` no existe en upstream) = sin conflicto. Si upstream añade workflows propios, conservar el nuestro y revisar colisiones de nombre |
| **`mateclaw-ui/vitest.config.ci.ts` (2026-09-15)** — config de vitest aditiva para CI que excluye 4 archivos de test que **ya fallan en upstream limpio `v2.1.0`** (verificado con `git worktree add … v2.1.0`: los mismos 8 asserts fallan allí). Sin la exclusión el CI nacería rojo y taparía fallos reales. Entradas a borrar cuando upstream los arregle: `product-cards`, `streaming-render`, `teamRunComponents`, `teamRunProjectionPrimitives` | Archivo nuevo (aditivo) = sin conflicto. **No** se tocó `vitest.config.ts` (del upstream) ni el script `test` de `package.json` |
| **Adopción de v2.2.0 (2026-09-15)** — primera integración de una versión estable desde el fork. Se mergeó el commit **`release: v2.2.0` de `upstream/dev`** (`08a5bf69`), no el tag: `upstream/main` está aplanado (squash) y su ancestro con nosotros es `v1.1.0` → `git merge v2.2.0` daba **313 conflictos**; el commit de `dev` da **4**. Además el tag de `main` es curado y omite archivos que `dev` sí tiene (los habría borrado). Contenido: runtime mainline (contrato de runtime de empleados + provider registry), DeepSeek Harness, Goals durables (cola/supervisor/leases/attempts), A2A bidireccional, OfficeCLI, ACP prompt timeout. Resultado: 288 archivos, +14103/-670, **0 archivos borrados**. Conflictos resueltos (4): `ConversationController` (unión import+campo), `ConversationControllerBatchDeleteTest` (unión), `McpServerServiceListToolsTest` (conservar ambos tests), `mateclaw-ui/src/types/index.ts` (estructura del upstream + campo `stream_progress` traducido). **El procedimiento está documentado en `AGENTS.md` §5 y §3 de este archivo** | **Superficie media**; `rerere` memorizó las 4 resoluciones. ⚠️ `main` del upstream NO se mergea nunca: usar siempre el commit de release de `dev` |
| **`fallbackLocale: 'en-US'` (2026-09-15)** — `mateclaw-ui/src/i18n/index.ts` tenía `fallbackLocale: DEFAULT_LOCALE` (= `es-ES`), así que ante una clave que `es-ES.ts` aún no traduce vue-i18n no tenía a dónde caer y renderizaba la **ruta cruda de la clave**. Con v2.2.0 son 37 claves nuevas. Ahora el fallback es `en-US` (el diccionario que el upstream mantiene completo) y `applyLocale` carga ese diccionario cuando el idioma activo no es inglés (los diccionarios son lazy: declarar el fallback no basta) | **Superficie baja**: `i18n/index.ts` (+constante exportada `FALLBACK_LOCALE`, +1 carga condicional). Si upstream toca ese archivo, conservar el fallback a `en-US` y la carga del diccionario de respaldo |
| **`spring.flyway.out-of-order: true` en `application-postgres.yml` (2026-09-15)** — nuestras migraciones son V900+ (regla 7) para no colisionar por nombre, pero eso deja la versión máxima aplicada en **901**, por encima de cualquier migración que el upstream añada después (v2.2.0 trae V186–V189). Con el default `out-of-order=false` Flyway se niega a aplicar esas pendientes de versión menor → el esquema nuevo nunca llega y el código actualizado falla en runtime. Nuestras V900/V901 son aditivas e independientes, así que aplicarlas tarde es seguro. *(Nota: la BD tenía V186 aplicado dos veces y las columnas `mate_agent.runtime_type/runtime_config` ya existían — quedó parcialmente pre-migrada de un deploy anterior; `validate-on-migrate: false` ya estaba puesto.)* | **Superficie baja**: 1 línea + comentario en un archivo del upstream. Re-aplicar si upstream toca el bloque `spring.flyway` de `application-postgres.yml`. Aplicar el mismo criterio en `application-kingbase.yml` si algún día se despliega sobre KingbaseES |
| **Test del upstream adaptado a nuestra firma (2026-09-15)** — v2.2.0 añade `ConversationControllerTeamWorkerTranscriptTest`, que construye el controlador con **3** argumentos; nuestra resolución del conflicto conservó ambos campos (`modelConfigService` nuestro + `teamWorkerGovernanceService` del upstream) → el constructor Lombok tiene **4** y el test no compilaba. Se le añadió el mock de `ModelConfigService` | **Superficie baja**: 1 archivo de test NUEVO del upstream. **Lección**: el merge no marca los archivos nuevos que asumen firmas viejas → correr siempre `mvn test-compile` antes de la suite (por eso está en el procedimiento, `AGENTS.md` §5.1 paso 5) |
| **Fix: páginas del Wiki perdidas al leer varias en una ronda (2026-09-16)** — bug **del upstream** (`jack`, 2026-06-16, commit `88be1f74` / #334) presente también en v2.1.0: `wiki_read_page` devuelve la página en un `title` de nivel superior y **sin índice**, pero `SourceEvidenceLedger.recordWikiEvidence` la registraba con **índice 1 hardcodeado**. Como `Builder.wikiCitation` ejecuta `removeIf(existing.index() == citation.index())`, la segunda página leída **borraba la primera** (y el `merge()` que hace `ActionNode` por ronda hacía lo mismo con las páginas leídas en rondas distintas). Efecto real: una respuesta que cita dos páginas recibía el aviso `[证据不足] … wiki citation [2]` aunque ambas se hubieran leído. Arreglo: (1) `Builder.nextFreeWikiIndex()` en lugar de la constante 1 → ninguna página se pierde y la tabla canónica del safety net ya no repite `[1]`; (2) `validateWikiCitations` deja de tratar el índice como identidad: si no resuelve, comprueba que la página apuntada esté entre las realmente leídas. **Citar una página nunca leída sigue rechazándose**; (3) `merge()` porta las citas nuevas con índices frescos y dedupe por título/chunkId en vez de evictar. | **Superficie baja**: `SourceEvidenceLedger` (`recordWikiEvidence` + `Builder.nextFreeWikiIndex` + `validateWikiCitations` + `merge`). Tests: +5 y se actualizó `rejectsWikiAnswerWithoutRealCitations`, que asertaba el comportamiento con bug (el índice es una etiqueta que el modelo elige libremente; lo que debe validarse es la página). **Reportar al upstream.** Re-aplicar si upstream reescribe el ledger |
| *(futuro)* `mateclaw-ui/...` | Título, i18n `es-ES`, textos visibles | Archivos aditivos (`es-ES.ts`) = sin conflicto; textos en `en-US.ts`/`zh-CN.ts` = re-aplicar |
| **Fix: las «Fuentes» de una respuesta de investigación aparecían como enlaces a `localhost` (2026-09-21)** — `useMarkdownRenderer.ts`: nueva opción **`wikiCitations`** en `RenderMarkdownOptions` (y en la clave de caché) que decide si la tabla de fuentes se trata como citas **wiki**; los anclajes `.wiki-citation` ya **no llevan `href="#"`** (eran la causa del síntoma: el navegador mostraba la URL del propio chat) y el marcador `[n]` gana `title` con el título de la página. `MessageBubble.vue` calcula el flag desde las **tool calls del turno** (`wiki_*` → es wiki; si no, no) | **Síntoma real:** en `conv_1790029096976_tsdw26` (investigación web con `progress_update` + `render_html_image`, **sin ninguna tool wiki**) el modelo escribió `Fuentes:\n[1] Worldometer — …` y la UI lo convirtió en `<a class="wiki-citation" href="#">`: al pasar el ratón el navegador mostraba `http://localhost:18080/chat?…#` y el clic buscaba una página wiki inexistente. **La tabla canónica del backend es `[n] <título>`: indistinguible de una lista de fuentes web**, por eso la decisión no puede ser por forma sino por contexto. Con el flag en `false` el texto queda intacto y las URLs reales de las fuentes se autoenlazan (GFM). Compatibilidad: `undefined` mantiene el comportamiento histórico para los demás consumidores del renderizador (Docs, Teams, WikiPageViewer, TeamRun…). Tests: `wikiCitations.test.ts` (5, incluye el texto verbatim del caso y el no-compartir caché entre ambas decisiones) |
| **Endurecimiento del endpoint de archivos + texto claro para el modelo (2026-09-21)** — (a) `GeneratedFileController.java`: CSP `sandbox` (+ `nosniff`) también para **`image/svg+xml`** (antes solo el HTML lo tenía): un SVG abierto como documento ejecuta sus `<script>` con nuestro origen. (b) `GeneratedFileLink.java`: nuevos `imageZh`/`imageEn` que devuelven el artefacto **ya en formato imagen** `![name](url)` y prohíben explícitamente envolverlo en backticks/bloques y **hablar del host**; `resultZh/resultEn` (documentos) reciben la misma advertencia; `HtmlImageRenderTool` y `ScreenshotTool` pasan a `imageZh`; `WorkspaceArtifactSurfacer` entrega `![name](url)` para artefactos de imagen (los PNG de matplotlib que produce `execute_code`), y `CodeExecuteTool`/`ShellExecuteTool` lo describen en su texto | Motivo: en el caso real el agente, al no ver la imagen, **especuló en el chat** sobre `localhost`, sobre que el usuario estuviera en otra máquina y sobre que el endpoint se hubiera roto — nada de eso era cierto. Ahora el tool le dice que la plataforma sirve y renderiza el archivo, y le entrega las imágenes listas para mostrar. **Re-aplicar si upstream toca estos textos/servicios**; los assets de imagen no dependen de que el modelo acierte el formato. Tests: `GeneratedFileLinkTextTest` (3), +2 de CSP en `GeneratedFileControllerTest`, +1 en `WorkspaceArtifactSurfacerTest` |
| **Fix: imágenes escritas como `![...](url)` no se veían (2026-09-21)** — `useMarkdownRenderer.ts`: **override de `image()`** (los tokens de imagen de markdown NO pasan por `link()`, que era el único que teníamos), `link()` ahora emite `data-generated-src` + píxel transparente, y ambos usan el nuevo helper compartido `sameOriginFilePath()` de `utils/generatedFileLinks.ts`. Además se aplica ese helper en `ConversationFilesPanel.vue` (miniaturas y `href`), `FilePreviewDialog.vue` (fetch) y `useGlobalGeneratedImageBlob.ts` (normaliza antes de pedir, como red de seguridad central) | **Síntoma real reportado:** el asistente respondió con `![poblacion-...png](http://localhost:18080/api/v1/files/generated/<id>)` y el chat no mostraba nada. Dos causas combinadas: (1) sin override de `image()`, la imagen salía como `<img>` normal **sin** `data-generated-image` → el loader autenticado la ignoraba → el endpoint exige token → **401**; (2) el host que el modelo repite (`localhost:18080`, el que el servidor conocía al crear el archivo) lo resuelve el navegador **contra sí mismo**, así que desde otra máquina nunca carga. Ahora toda imagen de la API de archivos va por el loader autenticado con **ruta relativa** de origen. ⚠️ `preview/__tests__/previewKind.test.ts` (test del upstream) ya quedó adaptado en la fila anterior. Test de regresión con el texto **verbatim** de la respuesta reportada. **Unificar siempre con `sameOriginFilePath`**: cualquier superficie nueva que descargue archivos generados debe normalizar el host |
| **Las imágenes generadas se ven en la UI (2026-09-21)** — `previewKind.ts` (tipo `'image'` + `IMAGE_EXTS` + `isImageName()`), NUEVO `components/chat/preview/ImagePreview.vue` (visor con zoom por botones y Ctrl+rueda, paneo arrastrando, y *abrir en pestaña* **solo para raster**), `FilePreviewDialog.vue` (rama `image`), `useGlobalFileDownloadClick.ts` (el clic en una imagen inline del mensaje abre **el visor**, antes abría un blob de mismo origen en pestaña nueva), `ConversationFilesPanel.vue` (miniaturas) e i18n (`chat.preview.zoomIn/zoomOut/zoomReset/openInTab` ×3) | Motivo: **las gráficas y elementos avanzados solo se podían DESCARGAR** — `previewKindOf()` no tenía especie para imágenes (devolvía `null` → rama de descarga), y la vista inline dependía de que el modelo repitiera el enlace con la extensión en la etiqueta. Ahora cualquier imagen (mensaje, panel, historial) **se abre en la interfaz**. Miniaturas del panel: montadas **perezosamente** con `IntersectionObserver` (el loader global hace fetch al insertar: montarlas todas descargaría todas las gráficas de la conversación) y con `data-generated-src` + pixel transparente para no disparar un 401 previo ni parpadear imagen rota; se reutiliza `useGlobalGeneratedImageBlob` (auth por blob). **Seguridad**: un SVG se renderiza siempre dentro de `<img>` y **nunca** como documento blob de mismo origen (sus `<script>` correrían con nuestra sesión); por eso no tiene «abrir en pestaña». ⚠️ **Contrato de test del upstream modificado**: `preview/__tests__/previewKind.test.ts` asertaba `null` para `pic.png` («handled elsewhere») → ahora `'image'`, con comentario explicando el porqué. **Al mergear el upstream, conservar nuestra expectativa** (si ellos añaden su propio visor de imágenes, preferir el suyo y retirar el nuestro) |
| **Se quitó la sección de archivos del panel del upstream (2026-09-21)** — `RunOverviewPanel.vue`: eliminados el `computed` `generatedFiles` (que leía **solo el último mensaje**, `latestAssistant`), su sección de plantilla, el badge del riel, `fileIconClass()` y su CSS (`run-overview__files/__file*/is-file`); `hasContent` ya no cuenta archivos (si no, el panel aparecería vacío) | Motivo: con el panel propio (`ConversationFilesPanel`) el mismo archivo aparecía **en los dos paneles** (el rail listaba el último turno, el nuestro toda la conversación) — redundancia visible al abrir una conversación antigua. Ahora **una sola fuente de verdad** para archivos; el rail queda para lo que le es propio: plan + subagentes del run actual. **Superficie**: borrado dentro de un archivo del upstream (sin tocar su API: mismos props). ⚠️ Las claves i18n `chat.runOverview.files` quedan **sin uso** en los 3 locales a propósito (si upstream reintroduce la sección, siguen ahí); si upstream reescribe ese componente, **conservar nuestra decisión** (verificar que no vuelva la sección de archivos) |
| **Vigencia configurable de los archivos generados (2026-09-21)** — `GeneratedFileCache.java` (`public static final Duration TTL = Duration.ofDays(7)` → `DEFAULT_TTL = Duration.ofDays(365)` + campo/setter `setTtl(Duration)` con `@Value("${mateclaw.generated-file.ttl:365d}")`, `ttl()`, `neverExpires()`, `expiryFor()`, `metaLine()` extraído de `persist()` y `extendLiveEntries()` con `@PostConstruct`), `GeneratedFileLink.java` (el texto que devuelve el tool usa el TTL efectivo: `365 days` / `indefinitely`), javadocs de `GeneratedFileController` y `GeneratedFileScrubber`, y `MATECLAW_GENERATED_FILE_TTL` en `docker-compose.yml` (+ `.env`) junto al presupuesto de disclosure | Motivo: **la caducidad a 7 días era una desventaja comercial** — regenerar un entregable cuesta otra corrida completa del modelo (tokens + espera), mientras que el disco es barato. Ahora es un parámetro: `365d` por defecto, `30d`/`90d` si se quiere acortar, **`0` = nunca caducan**. ⚠️ **El valor es POR DESPLIEGUE** (una sola variable para todo el servidor: todavía no hay TTL por plan ni por workspace — pendiente en `NEXT_SESSION.md`) (formato de duración de Spring: `365d`, `12h`, `P365D`). **Al arrancar, `extendLiveEntries()` AMPLÍA (nunca acorta) la vigencia de los archivos que siguen vivos** — idempotente y con log de cuántos extendió — así que subir el TTL recupera lo que aún no había caducado (lo ya borrado por el barrido es irrecuperable). **La caducidad borra los BYTES, no las referencias**: `metadata.generatedFiles` del mensaje se conserva, así que los nombres y enlaces de un archivo caducado siguen apareciendo en el historial y en el panel (solo fallan al hacer clic → 404). Tests nuevos: `GeneratedFileCacheTtlTest` (5) + 63 del área en verde | **Superficie**: 1 clase del upstream con cambios acotados (constante renombrada `TTL`→`DEFAULT_TTL`, campo inyectado, 2 helpers, `persist()` refactorizado a `metaLine()`), 1 clase de tool y 2 javadocs. **Re-aplicar si upstream toca `GeneratedFileCache`**: conservar el TTL configurable y `extendLiveEntries()`; si upstream ya trae su propia config de vigencia, migrar a la suya y borrar la nuestra |
| **Integración con Pi (F0, 2026-09-19)** — `docs/pi-integration/**` (10 documentos, ~1.630 líneas) | Documentación del plan de integración de **Pi** como motor dentro de AuraClaw. **Camino A**: tool `pi_worker` (genera PDF con gráficas, Office, vídeo, análisis). **Camino B**: `AgentRuntimeProvider` respaldado por Pi (sobre upstream v2.2.0). Decisión y anti-objetivos en `01`; contrato de runtime **verificado** contra `feature/upstream-v2.2.0` en `05`; seguridad en `06`; plan por fases en `08` | **Archivo nuevo (carpeta aditiva) = sin conflicto.** Código futuro en `com.auracore.*` (zona propia). Si upstream renombra `vip.mate.agent.runtime.contract.*` al integrar v2.2.0, revisar `05` y `09` y actualizar los nombres citados |
| **Diagnóstico del bucle de agentes + evaluación de DSH (2026-09-21)** — `docs/agent-runtime/**` (7 documentos: `README` + `01`…`06`) | Documentación medida del **fallo de rendimiento del agente** y de la evaluación del motor **DSH (DeepSeek Harness)** como alternativa. Contiene: (a) el diagnóstico con evidencia de logs de una pregunta simple que costó **241,5 s / 3.007.919 tokens / 62 iteraciones** con DeepSeek Flash, frente a 9 s del mismo modelo en un harness directo; (b) las **5 causas raíz** con `archivo:línea` (`ToolLoopGuard` sin `browser_use` en `IDEMPOTENT_TOOLS` y sin tratar 404 como fallo; `ReasoningNode:140` `KEEP_RECENT_TOOL_RESPONSES=3`; `toolSchemas=33222` con las **38 filas de `mate_tool` todas en `disclosure_tier='core'`**; `max_iterations=100` en los 4 agentes); (c) la **capa de medición** propuesta, que se apoya en datos **ya persistidos** (`mate_message.metadata.segments`, `mate_usage_daily.tool_call_count`) — demostrado reconstruyendo el turno desde la BD con 1–3 % de desvío; (d) el **spike de DSH** ejecutado end-to-end (37–58 s y 9 iteraciones con el mismo modelo) y sus **3 desajustes doc↔realidad**: el artefacto `dsh-jsonrpc-agent-pkg-<platform>` **no existe** (el real es `dist-exe/deepseek-harness-sdk-runtime-<platform>-<arch>` vía `scripts/build-exe-for-python-sdk.ts` + `pkg --sea`), **`DSH_CORDIS_CONFIG` no lo lee DSH** (0 referencias en 60 paquetes) y el auto-instalador es imposible (18 releases, **0 assets**); (e) la **vía corta verificada** (`DSH_JSONRPC_AGENT` apuntando a `node …/bin.js --profile sdk --patch …`, sin código Java); (f) el **defecto que bloquea usarlo como empleado**: `DshRuntimeService.java:245/296` genera un `sessionId` aleatorio por turno y envía **solo el mensaje actual** → sin memoria entre turnos (la versión con historial 40 msgs/4096 tokens que describe el doc del upstream **no está en nuestro `main`**); (g) el **puente de tools** como pieza que falta en los tres caminos (`DshToolDispatcher`/`Catalog`/`PolicyEvaluator` son **código muerto**: solo se referencian a sí mismos y a sus tests; AuraClaw **no tiene servidor MCP**, solo cliente); (h) plan por fases **M0–M6** con criterios de aceptación. Corrige además premisas desactualizadas de `docs/pi-integration`: `05` §1 y `08` dan F3 por bloqueado por el merge de v2.2.0, pero el contrato, el paquete `dsh/` y la migración `V186` **ya están en `main`** | **Archivo nuevo (carpeta aditiva) = sin conflicto.** ⚠️ Los documentos citan **números de línea** de archivos del upstream (`ToolLoopGuard.java:67/181`, `ReasoningNode.java:140/942`, `ConversationWindowManager.java:1128/1152`, `AgentGraphBuilder.java:421`, `DshRuntimeService.java:245/296`, `DshArtifactInstaller.java:171`): si upstream los reescribe, **verificar y actualizar la cita**, la causa suele seguir siendo válida aunque la línea se mueva. ⚠️ Las fases **M2** (toca `ToolLoopGuard`, `ReasoningNode`, `ConversationWindowManager`) y **M3** (toca `DshRuntimeService`) modifican **código del upstream** y por tanto **necesitan su propia fila aquí al implementarse**; M1/M4 van en `com.auracore.*` (zona propia). Al cambiar cualquier premisa, actualizar también `docs/pi-integration/05` §1/§7 y `08` (registro del mismo hallazgo) |
| **Panel de archivos por conversación (2026-09-21)** — `mateclaw-ui/src/utils/conversationFiles.ts` (nuevo, clasificación + dedupe), `mateclaw-ui/src/components/chat/ConversationFilesPanel.vue` (nuevo, panel colapsable) + 2 archivos de test, **3 líneas** en `mateclaw-ui/src/views/ChatConsole.vue` (import + `<ConversationFilesPanel :messages="messages" />` junto a `RunOverviewPanel`) y claves `chat.filesPanel.*` en los 3 i18n | Cubre la **mitad pendiente del issue #384 del upstream** ("no tener que revisar el historial"): la sección de archivos que trae el PR #447 mira **solo el último mensaje** (`RunOverviewPanel.vue:97` → `latestAssistant`), así que en conversaciones largas los archivos anteriores solo se encuentran releyendo el historial. El panel agrega **toda la conversación** desde `metadata.generatedFiles` (ya persistido en `mate_message.metadata`; el endpoint de mensajes devuelve todo si no se pasa `limit`), agrupa por turno y separa **entregables** de **intermedios** (colapsados tras un botón). Clasificación fail-open, en orden: (1) **citado en la respuesta final** — la señal más fuerte y la única que salva un entregable producido por `execute_code`; (2) tool de entrega (`send_file`, `renderPdf*`, `renderDocx*`, `render_xlsx`, `render_pptx`, `render_html_image`, `office_document`, `*_package`, `*_publish`, `capture_screenshot`); (3) extensión de documento, salvo nombres de scratch (`TEST_*`, `*_b64*`, `tmp_*`); (4) el resto = intermedio. Si un turno queda **sin** entregables se muestran todos (nunca una lista vacía). Filtrado contra datos reales: un turno con **19 entradas** (8 PNG + 7 `test_*` + `informe_final.md` + el mismo PDF **3 veces con URLs distintas**) se reduce a **1 entregable + 16 intermedios** | **Superficie mínima**: 2 archivos nuevos + 2 de test + 3 líneas en `ChatConsole.vue` + claves i18n aditivas. La lógica no depende del backend y pasa por la **costura `ConversationFilesSource`** (`messageMetadataFilesSource` es el default): cuando el upstream mergee el catálogo de artefactos (issue #514 / PR #539, hoy **abierto**) o exista un endpoint equivalente, se implementa esa misma firma y se pasa como prop `source` sin tocar el panel. **Si upstream publica su propio panel**: decidir conservar/cambiar/retirar el nuestro (componente aislado, retirarlo son 3 líneas + 2 archivos). Las descargas reutilizan el delegador global `useGlobalFileDownloadClick` (previsualización o descarga autenticada), así que **no** hay lógica de descarga propia. **No adivina** la caducidad (la vigencia es config del servidor: un constante en el cliente divergiría); en su lugar **aprende del 404**: `composables/useUnavailableFiles.ts` (registro reactivo por sesión) se alimenta desde `useGlobalFileDownloadClick` y desde el diálogo de previsualización cuando el servidor responde **404/410** —solo esos códigos, para no confundir el 501 de «sin convertidor de office»—; el panel pinta esas filas tachadas, sin enlace y con chip *no disponible*, más un contador en el riel, y ofrece un botón **«Ocultar no disponibles (N)»** persistido en `localStorage` (`mc-files-hide-unavailable`). Cero falsos positivos y auto-corrección (si el archivo vuelve a resolverse, no se marca). Tests: 11 + 6 + 5, sin tocar los 2 que montan `ChatConsole` (verificados). Relacionado: bug **#611** (el panel agrega solo mensajes de **su** conversación, así que no amplifica la fuga cross-workspace del upstream) |
| **`mateclaw-ui/scripts/check-snowflake-precision.{mjs,sh}` + 3 líneas de `mateclaw-ui/package.json` (2026-09-21)** — el upstream v2.1.0 (nuestra base) invocaba `bash ../scripts/check-snowflake-precision.sh`, script que **no existe en ningún release público**: `pnpm run build` y `pnpm run lint` fallaban de fábrica. Portado **verbatim del upstream v2.3.0** (el commit `8ea6326f` de `upstream/dev` introdujo una versión bash; v2.3.0 la sustituyó por la versión node, sin dependencia de `ripgrep`). 5 patrones que detectan truncado de ids Snowflake de 64 bits (`v-model.number` sobre `*Id`, `Number()`/`parseInt()` sobre ids o accesos a `*Id`, `<input type="number">` con `v-model` a un id, `typeof …Id === 'number'`), con allowlist por línea `// snowflake-precision-ok: <razón>` | Archivos nuevos dentro del paquete = aditivos, sin conflicto. Las 3 líneas de `package.json` quedan **textualmente idénticas a v2.3.0** → merge limpio al adoptarlo (verificado además contra el release commit `a2f35f7c` de `upstream/dev`: los dos archivos y las 3 líneas son byte a byte iguales, así que el merge es limpio venga del tag o de `dev`). Verificado sobre el código actual: `✓ Snowflake ID precision check: clean`. ⚠️ La línea `"version"` de ese `package.json` la mueve upstream en cada release (conflicto trivial) |
| **`mateclaw-desktop/scripts/download-jre.sh` (reescrito, 2026-09-21)** — el script del upstream era **solo-macOS** (`ADOPTIUM_BASE` con `/mac` fijo) pese a que su propio `scripts/README.md` documentaba descargas `win-x64`/`win-arm64` y `build-all-platforms.sh` prometía empaquetado para Windows. Ahora acepta `mac-arm64`, `mac-x64`, `linux-x64`, `linux-arm64`, `win-x64`, `win-arm64`, varios targets a la vez, `all` (ambas de macOS, comportamiento legacy), `all-platforms`, `--dry-run` y `-h/--help`; `JRE_DIR=<dir>` redirige la salida. Autodetecta OS+arch (`Darwin/Linux/MINGW*/MSYS*/CYGWIN*` × `arm64/x86_64`), elige `tar.gz` (mac/linux) o `zip` (Windows), normaliza el layout (`mac-*` conserva `Contents/Home`; linux/win planos) y crea el symlink `resources/jre/win32-x64 → win-x64` que `electron/main/index.ts` busca en modo dev | Archivo del upstream **reescrito por nosotros**: si upstream lo vuelve a tocar, conservar nuestra versión y re-aplicar los bloques nuevos del upstream (matriz de URLs y layout). Validado de verdad: descargas reales de `linux-x64`, `linux-arm64` y `win-x64` (Temurin 21.0.12.1), `bin/java` ejecutable y layout verificado; las 6 URLs de Adoptium responden 200 |
| **`mateclaw-desktop/scripts/build-all-platforms.sh` (reescrito, 2026-09-21)** — el original llamaba a `npx electron-builder` directamente: **nunca construía el frontend** (`npm run build`) ni descargaba el JRE por plataforma, así que empaquetaba un `dist-electron/` obsoleto y sin JRE fuera de macOS; tampoco aceptaba `--mac-only`/`--win-only` que su README anunciaba. Ahora ejecuta la pipeline documentada (JAR → JRE por plataforma → frontend → electron-builder), soporta `--local|--remote` × `--mac-only|--win-only|--linux-only|--all-platforms`, y **omite JAR+JRE en `--remote`** (que electron-builder no empaqueta) | Archivo del upstream **reescrito por nosotros**: conservar nuestra versión en conflicto |
| **`mateclaw-desktop/package.json` (2026-09-21)** — scripts nuevos `package:linux`, `package:linux:local`, `package:linux:remote` (el target `linux: AppImage` ya existía en `electron-builder.cjs`, pero ningún script lo invocaba). `package:linux` queda redactado **igual que la forma más probable** con la que upstream lo añada | 3 líneas nuevas; conflicto trivial si upstream publica el mismo script con otro texto |
| **`mateclaw-desktop/scripts/README.md` (2026-09-21)** — documentados los targets de JRE para Linux/Windows, `--dry-run`, el symlink `win32-x64`, los flags de `build-all-platforms.sh` y `package:linux`. (El texto del upstream ya describía cosas que sus scripts no hacían; ahora coinciden) | Documento del upstream **con prosa nuestra**: conservar nuestra versión en conflicto |

### Sustituciones de branding (apply después de cada merge si hace falta)

```bash
# Prosa de marca en README (NO toca rutas lowercase ni nombres de módulos)
sed -i 's/MateClaw/AuraClaw/g' README.md
```

> ⚠️ **No renombrar** por ahora: paquetes Java (`vip.mate.*`), nombres de módulos Maven,
> artefactos/JARs, esquema de BD, contenedores docker, variables de entorno (`MATECLAW_*`),
> nombres de archivos de logo (`mateclaw_logo_*.png` — se reemplaza solo el CONTENIDO).
> Son identificadores internos invisibles al usuario final y cambiarlos genera conflictos
> en cada merge sin beneficio visible.

## 3. Procedimiento de actualización (cuando upstream publique una versión estable)

> ⚠️ **El tag NO se mergea.** `upstream/main` es una línea aplanada (squash) cuyo
> ancestro común con nosotros es `v1.1.0`: `git merge v2.2.0` da **313 conflictos**.
> Se mergea el commit `release: vX.Y.Z` de `upstream/dev`, que **sí desciende de
> nuestra base** → **4 conflictos** (medido 2026-09-15). Detalle completo en `AGENTS.md` §5.

```bash
# 1. Traer lo nuevo del oficial (tags + ramas)
git fetch upstream --tags

# 2. Localizar el commit de release en DEV (no el tag)
UP=$(git log --format='%H' upstream/dev --grep='^release: v2.2.0$' -1)
git log -1 --format='%ci %s' "$UP"      # verificar antes de mergear

# 3. Integrar en una rama (merge, nunca rebase)
git checkout -b feature/upstream-v2.2.0 main
git merge "$UP"

# 4. Resolver conflictos (rerere re-aplica los ya vistos)
#    - unión de imports/campos; conservar nuestros tests Y añadir los suyos
#    - re-aplicar español en los archivos de UI que upstream toque

# 5. Re-aplicar branding en los archivos NUEVOS del upstream
grep -rl "MateClaw" --include='*.md' mateclaw-server/src/main/resources/docs \
  | xargs sed -i 's/MateClaw/AuraClaw/g'
#    (NO tocar identificadores java, ni el aviso de fork del README)

# 6. Verificar ANTES de mergear a main
mvn -q test-compile -pl mateclaw-server     # los tests nuevos pueden asumir firmas viejas
mvn test -pl mateclaw-server                # suite completa (~4800 tests, ~15 min)
cd mateclaw-ui && npx vue-tsc --noEmit && npx vitest run --config vitest.config.ci.ts

# 7. Verificar en vivo (rebuild Docker + Flyway aplicando las migraciones nuevas)

# 8. Publicar
git checkout main && git merge feature/upstream-v2.2.0
git tag v2.2.0-mc.1                        # tag propio por versión integrada
git push origin main --tags
```

## 4. Checklist pre-release comercial

- [ ] Merge del tag estable más reciente (sección 3)
- [ ] Branding re-aplicado (README, UI, logos)
- [ ] `mvn test` (server) + `npm run test` (ui) verdes
- [ ] `git tag vX.Y.Z-mc.N` creado y pusheado
- [ ] Upgrade probado desde la versión anterior (migraciones Flyway `V9xx+` propias si las hay)
